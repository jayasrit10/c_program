#include <stdio.h>
int main()
{
    float x,sub;
    printf("enter the num:");
    scanf("%f",&x);
    sub= x-5;
    printf("the new after subtracting the given value with 2:%.2f",sub);
    return 0;
}